---
import games from '../data/games.json'
import GameLayout from '../layouts/GameLayout.astro'

export function getStaticPaths() {
	return games.map((game) => ({
		params: { gameId: game.id },
		props: { game },
	}))
}

const { game } = Astro.props
---

<GameLayout title={game.title}>
	<div class='h-dvh border-4 border-cream-300'>
		<iframe
			src={game.gameUrl}
			id='game-iframe'
			width='800'
			height='450'
			class='w-full h-full'
			allowfullscreen='true'></iframe>
	</div>

	<div class='py-l text-center'>
		<a
			href='/'
			aria-label='home'
			class='inline-block bg-cream-200 hover:bg-cream-300 border-2 border-cream-100 px-xs py-2xs font-kode font-bold'
			>Back to games
		</a>
	</div>
</GameLayout>
